# PROMPTS — System prompts de Gemini

> Estado: **Pendiente** — definir cuando el módulo de IA arranque.

## Convenciones

- Cada prompt tiene un nombre, versión, y descripción de cuándo usarlo.
- Al cambiar un prompt, incrementar la versión y dejar el anterior comentado con la fecha.
- Los prompts se cargan desde este archivo (o desde variables de entorno) — nunca hardcodeados en el código.

---

## `CAREER_COACH_SYSTEM` v0.1

> Estado: borrador — no implementado aún.

```
(definir aquí el system prompt principal de DulIA)
```

---

## `JOB_MATCHER_SYSTEM` v0.1

> Estado: borrador — no implementado aún.

```
(definir aquí el prompt para matching de ofertas laborales)
```

---

_Agrega un bloque por cada prompt distinto que use el sistema._
